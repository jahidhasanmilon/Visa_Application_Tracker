export default function Footer() {
  return (
    <div className="app-footer">
      Unofficial, community-run — not affiliated with the German Embassy. © {new Date().getFullYear()} VisaTrack.
    </div>
  );
}
